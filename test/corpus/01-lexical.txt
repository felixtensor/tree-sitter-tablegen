==================
Line comment is ignored
==================

// hi
def X;

---

(source_file
  (def_definition (identifier)))

==================
Block comment is ignored
==================

/* outer */
def X;

---

(source_file
  (def_definition (identifier)))

==================
Nested block comment (ProgRef permits)
==================

/* outer /* inner */ outer */
def X;

---

(source_file
  (def_definition (identifier)))

==================
Decimal hex binary integer literals
==================

def X { int a = 42; int b = 0xFF; int c = 0b1010; int d = -7; int e = +3; }

---

(source_file
  (def_definition
    (identifier)
    (body
      (field_declaration (type) (identifier) (integer_literal))
      (field_declaration (type) (identifier) (integer_literal))
      (field_declaration (type) (identifier) (integer_literal))
      (field_declaration (type) (identifier) (integer_literal))
      (field_declaration (type) (identifier) (integer_literal)))))

==================
String literal with escapes and concatenation
==================

def X { string s = "a\nb" "c\td"; }

---

(source_file
  (def_definition
    (identifier)
    (body
      (field_declaration (type) (identifier) (string_literal)))))

==================
Boolean and unset literals
==================

def X { bit a = true; bit b = false; bit c = ?; }

---

(source_file
  (def_definition
    (identifier)
    (body
      (field_declaration (type) (identifier) (boolean_literal))
      (field_declaration (type) (identifier) (boolean_literal))
      (field_declaration (type) (identifier) (unset_value)))))

==================
Identifier with leading digit
==================

def 3rdX;

---

(source_file
  (def_definition (identifier)))
