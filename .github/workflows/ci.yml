name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - LICENSE
      - README.md
      - docs/**
      - .editorconfig
      - .gitattributes
      - .gitignore
  pull_request:
    paths-ignore:
      - LICENSE
      - README.md
      - docs/**
      - .editorconfig
      - .gitattributes
      - .gitignore

concurrency:
  group: ${{github.workflow}}-${{github.ref}}
  cancel-in-progress: true

env:
  # GitHub hosted runners force JavaScript actions to Node 24 from 2026-06-02.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    name: Test parser
    runs-on: ${{matrix.os}}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up tree-sitter
        uses: tree-sitter/setup-action/cli@v2

      # Tier 1: Corpus tests & Binding tests
      - name: Run parser and binding tests
        uses: tree-sitter/parser-test-action@v3
        with:
          test-rust: true
          test-node: true
          node-version: "22"
          test-python: true
          test-go: true
          test-swift: true
          test-zig: true

      # Parse examples without ERROR nodes as a required integration corpus.
      - name: Parse examples
        uses: tree-sitter/parse-action@v4
        with:
          files: examples/**/*.td
